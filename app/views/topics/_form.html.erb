<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :トピックス %><br />
    <%= f.text_area :contents, :size=>"60x4"%>
  </div>
  <div class="field">
    <%= f.label :表示期限 %><br />
    <%= f.date_select :effective_to %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
